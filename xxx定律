#include<stdio.h>
#include<math.h>
int main()
{
	int test;
	while(scanf("%d",&test)!=EOF&&test)
	{
		int t=0;
		while(test!=1)
		{
			if(test%2)
			{
				test=(3*test+1)/2;
				t++;
			}
			else
			{
				test/=2;
				t++;
			}
			
		}
		printf("%d\n",t);
	}
}
